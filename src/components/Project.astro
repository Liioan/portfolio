---
import MagneticButton from "./MagneticButton";
import { type projectType } from "../pages/index.astro";
interface Props {
  project: projectType;
}
const { project } = Astro.props;
---

<div
  class="flex min-h-[480px] w-[70%] max-w-md flex-col justify-between gap-10 rounded-[20px] border-2 border-[#f5f5f5a0] bg-gradient-to-br from-[#f5f5f50f] p-10"
>
  <div class="flex flex-col gap-2">
    <h2 class="text-[40px] font-bold text-white">{project.title}</h2>
    <h3 class="text-[20px] font-semibold text-primary">
      {project.subtitle}
    </h3>
  </div>
  <p class="font-content text-[20px] font-semibold text-white">
    {project.description}
  </p>
  <div class="flex gap-[25px]">
    <MagneticButton client:only>
      <a href={project.github} class="hide-cursor">
        <img src="/github2.png" alt="github" />
      </a>
    </MagneticButton>
    {
      project.website && (
        <MagneticButton client:only>
          <a href={project.website} class="hide-cursor">
            <img src="/external.png" alt="external" />
          </a>
        </MagneticButton>
      )
    }
  </div>
</div>
